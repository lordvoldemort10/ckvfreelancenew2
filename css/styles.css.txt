/* Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Algemene stijl voor een rustige, renaissance-uitstraling */
body {
    font-family: 'Georgia', serif; /* Elegant en professioneel lettertype */
    color: #3b302a;
    background-color: #faf3e0; /* Zachte, licht beige achtergrondkleur */
}

/* Header styling */
header {
    background-color: #e4d1a2; /* Lichte, klassieke kleur */
    color: #3b302a;
    padding: 2em 0;
    text-align: center;
}

header h1 {
    font-size: 3em;
    font-family: 'Palatino', serif;
    color: #3b302a;
}

header p {
    font-size: 1.2em;
    margin-top: 0.5em;
    color: #6b4f4f;
}

/* Custom button styles */
.btn-custom {
    background-color: #a58d67; /* Zachte, neutrale kleur */
    color: white;
    border-radius: 8px;
    padding: 10px 20px;
    font-weight: bold;
    transition: background-color 0.3s ease;
}

.btn-custom:hover {
    background-color: #8c7153;
    transform: translateY(-3px);
}

/* Gallery styling */
.gallery {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 20px;
    padding: 20px;
}

.gallery img {
    width: 100%;
    height: auto;
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Lichte schaduw */
    margin-bottom: 15px;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.gallery img:hover {
    transform: scale(1.05);
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
    cursor: pointer;
}

/* Footer styling voor een rustige en elegante uitstraling */
footer {
    background-color: #f2e8dc; /* Licht beige, renaissance-stijl kleur */
    color: #3b302a; /* Donkerbruine tekstkleur */
    padding: 2em 0;
    text-align: center;
    font-size: 0.9em;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
}

.footer-socials {
    display: flex;
    justify-content: center;
    align-items: center;
    margin-bottom: 10px;
}

footer a {
    color: #3b302a;
    text-decoration: none;
    margin: 0 10px;
    transition: color 0.3s;
}

footer a:hover {
    color: #6b4f4f; /* Iets lichtere tint bij hover */
}

footer i {
    font-size: 1.5em;
    margin: 0 5px;
}

.footer-text {
    font-family: 'Georgia', serif; /* Elegant lettertype */
}

/* Hero section styling */
.hero-section {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 80vh;
    background-image: url('../images/hero-background.jpg');
    background-size: cover;
    background-position: center;
    color: white;
    text-align: center;
    flex-direction: column;
    animation: fadeIn 1.5s ease-out;
}

.hero-section h1 {
    font-size: 3.5em;
    margin-bottom: 0.5em;
    text-shadow: 1px 1px 6px rgba(0, 0, 0, 0.3);
}

.hero-section p {
    font-size: 1.5em;
    max-width: 700px;
}

/* Nieuwe visuele secties met iconen */
.section-title {
    color: #00539C;
    font-size: 2em;
    font-weight: bold;
    margin-bottom: 20px;
}

.icon {
    font-size: 1.2em;
    margin-right: 10px;
    color: #67a6ff;
}

/* Keyframe animations */
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}

/* Responsive design */
@media (max-width: 768px) {
    header h1 {
        font-size: 2em;
    }

    .gallery img {
        width: calc(50% - 10px);
    }

    nav {
        flex-direction: column;
    }

    nav a {
        margin: 5px 0;
    }
}

@media (max-width: 480px) {
    header h1 {
        font-size: 1.5em;
    }

    .gallery img {
        width: 100%;
    }
}
